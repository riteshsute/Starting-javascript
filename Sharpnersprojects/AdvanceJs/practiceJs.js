class Student{
    constructor(name, age, marks)(
    this.name= name;
    this.age age:
    this.marks marks;
    }
    setPlacementAge(minPlacement Age){
    console.log(this);
    return (minMarks) => (
    console.log('inside egligibleForCurrent Company'.this)
    if(this.marks > minMarks && this.age > minPlacement Age){
    console.log(this.name "is ready for placements")
    }else {
    console.log(this.name+"is not ready for placements")
    }